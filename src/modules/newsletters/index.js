const controllers = require("./controllers");
const routes = require("./newsletters.routes");
const service = require("./newsletters.service");

module.exports = {
  controllers,
  routes,
  service,
};
