const controllers = require("./controllers");
const routes = require("./saved-jobs.routes");
const service = require("./saved-jobs.service");

module.exports = {
  controllers,
  routes,
  service,
};
